{
  "name": "impensa-server",
  "version": "1.0.69",
  "description": "Backend",
  "main": "index.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "cd ../client && npm install && npm run build",
    "start": "node ./index.js",
    "dev": "npx nodemon ./index.js"
  },
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
